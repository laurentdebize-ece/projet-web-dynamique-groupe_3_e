document.getElementById("ajout-panier-form").addEventListener("submit", function(event) {
    event.preventDefault(); // Empêche le comportement par défaut de l'événement submit
    // Ajoutez ici votre logique d'ajout au panier avec JavaScript
});